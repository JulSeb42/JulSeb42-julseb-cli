{{#if forward}}
/*=============================================== {{ pascalCase name }} component ===============================================*/

import { forwardRef } from "react"
import { Styled{{ pascalCase name }} } from "./styles"
import type { I{{ pascalCase name }} } from "./types"

export const {{ pascalCase name }} = forwardRef<HTML{{ pascalCase attribute }}Element, I{{ pascalCase name }}>(({ {{#if children}} children,{{/if}}{{#if as}} as,{{/if}} }, ref) => {
    return (
        <Styled{{ pascalCase name }} ref={ref}{{#if as}} as={as}{{/if}}>
            {{#if children}}{children}{{/if}}
        </Styled{{ pascalCase name }}>
    )
})
{{else}}
/*=============================================== {{ pascalCase name }} component ===============================================*/

import { Styled{{ pascalCase name }} } from "./styles"
import type { I{{ pascalCase name }} } from "./types"

export const {{ pascalCase name }}: FC<I{{ pascalCase name }}> = ({ {{#if children}} children,{{/if}}{{#if as}} as,{{/if}} }) => {
    return (
        <Styled{{ pascalCase name }}{{#if as}} as={as}{{/if}}>
            {{#if children}}{children}{{/if}}
        </Styled{{ pascalCase name }}>
    )
}
{{/if}}