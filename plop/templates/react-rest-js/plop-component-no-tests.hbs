/*=============================================== Generate component ===============================================*/

const generateComponent = (/** @type {import('plop').NodePlopAPI} */ plop) => {
    const { setGenerator } = plop

    setGenerator("component", {
        description: "React component",
        prompts: [
            {
                type: "input",
                name: "name",
                message: "Enter component's name",
                validate: data => {
                    if (!data) return "Name is required"
                    return true
                },
            },
            {
                type: "input",
                name: "tag",
                message: "Enter HTML tag",
                default: "div",
            },
            {
                type: "confirm",
                name: "forward",
                message: "Add `forwardRef`?",
                default: false,
            },
            {
                type: "confirm",
                name: "as",
                message: "Add `as` prop?",
                default: false,
            },
            {
                type: "confirm",
                name: "children",
                message: "Add `children` prop?",
                default: true,
            },
        ],
        actions: [
            {
                type: "addMany",
                destination:
                    "../client/src/components/{{surroundBrackets "pascalCase name" }}",
                templateFiles: "./templates/component/*.hbs",
                base: "./templates/component",
            },
            {
                type: "modify",
                path: "../client/src/components/index.js",
                template:
                    'export * from "components/{{surroundBrackets "pascalCase name" }}"\n$1',
                pattern: /(\/\/ prependHere)/g,
            },
        ]
    })
}

module.exports = { generateComponent }
