{{#if props}}
/*=============================================== {{ pascalCase name }} component ===============================================*/

export const {{ pascalCase name }}: FC<I{{ pascalCase name }}> = ({}) => {
    return (
        <{{ lowerCase tag }}>
            
        </{{ lowerCase tag }}>
    )
}

interface I{{ pascalCase name }} {

}
{{else}}
/*=============================================== {{ pascalCase name }} component ===============================================*/

export const {{ pascalCase name }}: FC = () => {
    return (
        <{{ lowerCase tag }}>
            
        </{{ lowerCase tag }}>
    )
}
{{/if}}