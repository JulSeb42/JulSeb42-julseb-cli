/*=============================================== {{ pascalCase name }} model ===============================================*/

import { Schema, model } from "mongoose"
import type { {{ pascalCase name }} } from "../../shared/types"

const userSchema = new Schema<{{ pascalCase name }}>(
    {
        
    },
    { timestamps: true }
)

export const UserModel = model<User>("User", userSchema)
