/*=============================================== {{ pascalCase name }} model ===============================================*/

import { Schema, model } from "mongoose"
import type { {{ pascalCase name }} } from "../../shared/types"

const {{ camelCase name }}Schema = new Schema<{{ pascalCase name }}>(
    {
        
    },
    { timestamps: true }
)

export const {{ pascalCase name }}Model = model<{{ pascalCase name }}>("{{ pascalCase name }}", {{ camelCase name }}Schema)
