/*=============================================== Exports ===============================================*/

const { UserModel } = require("./User.model")
/* prepend import - do not remove */

module.exports = {
    UserModel,
    /* prepend export - do not remove */
}
