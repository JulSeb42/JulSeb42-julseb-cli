/*=============================================== {{ pascalCase name }} component ===============================================*/

{{#if forward}}

import { forwardRef } from "react"
import { Styled{{ pascalCase name }} } from "./styles"

export const {{ pascalCase name }} = forwardRef(({ {{#if children}} children,{{/if}}{{#if as}} as,{{/if}} }, ref) => {
    return (
        <Styled{{ pascalCase name }} ref={ref}{{#if as}} as={as}{{/if}}>
            {{#if children}}{children}{{/if}}
        </Styled{{ pascalCase name }}>
    )
})

{{else}}
import { Styled{{ pascalCase name }} } from "./styles"

export const {{ pascalCase name }} = ({ {{#if children}} children,{{/if}}{{#if as}} as,{{/if}} }) => {
    return (
        <Styled{{ pascalCase name }}{{#if as}} as={as}{{/if}}>
            {{#if children}}{children}{{/if}}
        </Styled{{ pascalCase name }}>
    )
}
{{/if}}
