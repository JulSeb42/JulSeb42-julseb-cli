/*=============================================== {{ pascalCase name }} model ===============================================*/

const { Schema, model } = require("mongoose")

const {{ pascalCase name }}Schema = new Schema(
    {
        
    },
    { timestamps: true }
)

const {{ pascalCase name }}Model = model("{{ pascalCase name }}", {{ pascalCase name }}Schema)

module.exports = { {{ pascalCase name }}Model }
