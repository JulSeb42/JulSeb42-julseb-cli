/*=============================================== {{ pascalCase name }} model ===============================================*/

import { Schema, model } from "mongoose"

const {{ camelCase name }}Schema = new Schema(
    {
        
    },
    { timestamps: true }
)

export const {{ pascalCase name }}Model = model("{{ pascalCase name }}", {{ camelCase name }}Schema)
