/*=============================================== {{ pascalCase name }} model ===============================================*/

const { Schema, model } = require("mongoose")

const {{ camelCase name }}Schema = new Schema(
    {
        
    },
    { timestamps: true }
)

const {{ pascalCase name }}Model = model("{{ pascalCase name }}", {{ camelCase name }}Schema)

module.exports = { {{ pascalCase name }}Model }