/*=============================================== {{ pascalCase name }} ===============================================*/
{{#if components}}

import { {{components}} } from "components"
{{/if}}

export function {{ pascalCase name }}({{#if props}}{ }{{/if}}) {
    return (
        {{#if tag}}
        <{{ tag }}>

        </{{ tag }}>
        {{else}}
        <>

        </>
        {{/if}}
    )
}
