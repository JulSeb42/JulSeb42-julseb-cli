/*=============================================== {{ pascalCase name }} component ===============================================*/

export const {{ pascalCase name }} = ({{#if props}}{ }{{/if}}) => {
    return (
        <{{ lowerCase tag }}>
            
        </{{ lowerCase tag }}>
    )
}
